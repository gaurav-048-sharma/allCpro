#include<stdio.h>
int main() {
    signed char chr;
    chr = 128;
    printf("The value of variable chr is %d\n",chr);
    // int x,r,s=0,y;
    // printf("Enter the 3 digit values of x : ");
    // scanf("%d",&x);
    // y=x;
    // while (x!=0) {
    //     r=x%10;
    //     s= (s*10)+r;
    //     x=x/10;
    // }
    // printf("%d " , s);
    // if(s==y) {
    //     printf("\nThe given number is a palindrome.");
    // } else {
    //     printf("\nThe given number is not a palindrome.");
    // }
}